version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm install
      - npm run clean || echo "Clean step skipped"
      - npm run build
run:
  runtime-version: 18
  command: npm start
  network:
    port: 8080
    env: PORT
  env:
    - name: NODE_ENV
      value: production
EOF
